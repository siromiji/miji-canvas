import { Background } from './Background';
import { Toolbar } from './Toolbar';

export const Edit = () => {
    return (
        <>
            <Background />
            <Toolbar />
        </>
    );
};
